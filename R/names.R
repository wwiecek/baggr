# fancy display names in the package
model_names <- c("mutau" = "Aggregate data (with control group)",
                 "rubin" = "Aggregate data (Rubin)",
                 "full" = "Individual-level data",
                 "quantiles" = "Effects on quantiles")
